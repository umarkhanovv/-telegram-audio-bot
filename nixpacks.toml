[phases.setup]
nixPkgs = ["ffmpeg", "python311Full"]

[phases.install]
cmds = ["python3 -m ensurepip", "python3 -m pip install -r requirements.txt", "python3 -m pip install yt-dlp"]

[start]
cmd = "python3 main.py"
